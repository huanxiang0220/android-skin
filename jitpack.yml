jdk:
  - openjdk17

install:
  - sdk install gradle 7.6
  - gradle --no-daemon clean :skin-base:publishToMavenLocal :skin-compat:publishToMavenLocal :skin-constraintlayout:publishToMavenLocal :skin-material-design:publishToMavenLocal -x test -x lint
